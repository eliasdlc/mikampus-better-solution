{$DOMAIN} {
  encode zstd gzip

  # El login real de estudiantes llega en la Fase 3. Durante el piloto
  # single-user esta barrera protege toda la app, incluida la API mutante.
  basic_auth {
    {$BASIC_AUTH_USER} {$BASIC_AUTH_PASSWORD_HASH}
  }

  reverse_proxy app:4173
}
